#!/bin/bash
# Skip e2e tests in pre-commit hook - they require a running dev server
# Run e2e tests manually with: bun run test:e2e
# Or use --no-verify flag to skip this hook entirely: git commit --no-verify

# Only run linting in pre-commit
bun run lint || true
