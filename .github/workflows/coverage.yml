name: Coverage

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    env:
      CI: 'true'
      VITE_COVERAGE: 'true'
      PUBLIC_CONVEX_URL: http://127.0.0.1:3210
      PUBLIC_CONVEX_SITE_URL: http://127.0.0.1:3211
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.6

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Playwright browser
        run: bunx playwright install --with-deps chromium

      - name: Component coverage
        run: |
          printf 'PUBLIC_CONVEX_URL=%s\nPUBLIC_CONVEX_SITE_URL=%s\n' "$PUBLIC_CONVEX_URL" "$PUBLIC_CONVEX_SITE_URL" > .env
          bun run test:coverage:component

      - name: Convex coverage
        run: bun run test:coverage:convex

      - name: E2E coverage
        run: bun run test:coverage:e2e

      - name: Merge coverage
        run: bun run coverage:merge

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 30
